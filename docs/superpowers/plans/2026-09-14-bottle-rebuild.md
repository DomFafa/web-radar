# Bottle rebuild and independent scene assets

The user authorized fixing design fidelity, adding generated scene materials, deploying the application, and redoing the bottle website. Preserve the image-model-first design flow, existing approved facts, source constraints, private product images and all unrelated work.

## Changes

- Preserve form binding selectors through trusted control replacement.
- Inspect actual page type, card columns/descriptions and form layout before conversion; reject an About image that is a catalog.
- Generate standalone photographic scenes with the dedicated Image 2.5 provider from approved scene references and original product photos. Keep webpage UI as live HTML. Save private assets, enforce character/byte limits and prevent blind paid retries.
- Bind prepared scenes through server-owned IDs and verify the final rendered page uses the actual generated pixels. Keep product-card/detail photos original.
- Count and identify physical products in each approved scene before generation. Review the generated scene against that composition and the original product photos; allow one explicit correction only for a known rejected image, never for an uncertain network response. Separate each product's original studio-shot instructions from the combined scene's count and arrangement.
- Bind short presentation labels from approved localized text and use a fixed trusted icon set. Reject unbound visible text, removed model SVG, missing labels and empty labels before accepting the website.
- Validate every design before generating HTML. Build home first; build at most two inner pages concurrently with cancellation and shared home navigation/styles.

## Acceptance and release

Run targeted regressions, the Python suite and type checks, and npm run check. Freeze only the builder runtime and unchanged vendor/dependencies; patch only the deployed Worker page prompt and original-reference handoff. Preserve frontend assets, bindings, auth, routes, database formats and unrelated services. Check idle jobs, retain code/environment/SQLite rollback evidence, deploy builder before Worker, and verify actual running bytes and authenticated endpoints.

Retain the existing bottle project/build and media in private evidence. Regenerate its incorrect About design through the normal production job API, inspect the resulting image, then attempt a fresh production build with the new path. Inspect generated materials and all desktop/mobile pages. Report actual outcome and timings; health and tests alone do not establish visual success. Customer-site public publication is separate from this application deployment and preview rebuild.

## First production retry evidence

Builder v1 and the narrow Worker changes were deployed. About was regenerated as a company page, and build `bf9abce4-774e-42f8-b8cc-22dbf97af8ac` completed with 12 files and four scene assets. Actual screenshots rejected that result: home photography reduced three physical bottles to one, while unbound Unicode icons and short labels disappeared during sanitation. The added composition review and trusted presentation bindings address those observed failures. The v1 result is retained for comparison and is not recorded as visually accepted.

## Label planning correction

Build `910e2c40-5e80-49d5-906a-03f9b7c53ef3` stopped in preflight because freely written labels failed exact approved-quote validation; no image stage started. The planner now selects per-language IDs from a system-generated, validated phrase list. The server supplies the matching words before normal binding validation. Candidates retain nearby qualifications, contain at most six words/60 characters, and are limited to 100 per language. This prevents freely paraphrased labels and long material descriptions from entering the short-label contract.

The updated planner passed a real bottle-home canary in 6.91 seconds with four short labels, one text/vision call and zero image calls. Full Python validation passed 357 tests; after the final candidate-length restriction, 74 relevant tests and the complete type check passed. The next production build still requires actual scene and browser acceptance.

## Catalog assignment correction

Build `3dd746d0-aaeb-4c7b-a39e-0eee2af0fd47` stopped before images on invalid catalog product-ID membership. The planner now receives a strict required object key for each approved product; it selects only approved section indices. The program constructs groups in original section/product order, guaranteeing every product appears once. Separate schema alternatives allow either all-null ungrouped membership or all-numeric section membership. Existing assembled-page contracts remain unchanged.

All five real bottle-design planning calls then passed in 36.32 seconds total, with five text/vision calls and zero image calls. The catalog returned three groups of 2/2/4 products, covering all eight exact IDs once. The final 52 focused regressions, complete type check and diff check passed. The unchanged runtime components retain the earlier 357-test full-suite evidence.

## Catalog scene and review correction

Build `79398050-a052-41ca-a630-5f532be25a60` passed planning, home scene and home/detail browser checks, then rejected both catalog scene attempts. Actual images showed four physical bottles. The source numeric audit incorrectly counted a packaging illustration as a fifth bottle; typed object inventory now computes the count from physical products only. A two-call real text audit verified both source and old output count four, while the old output still failed composition.

The source crop is 757×357. Fixed 1536×1024 output changed its geometry and introduced a plant pot. The current Image 2.5 API supports custom dimensions, so the candidate requests 1536×720, preserving the wide scene and its blank region. The isolated canary produced a materially faithful four-bottle image, but its first review compared different input resolutions and incorrectly reported large scale/framing changes. It then exceeded the 28,000-character prompt limit when the correction included a duplicate object inventory. Neither that correction nor another full build was submitted.

In a controlled review experiment, the same source and new output were contained on equal 1536×736 canvases without altering the actual material. With the same review instructions and gates, the new output passed all checks in 8.27 seconds. The old output, treated identically, still failed composition in 9.85 seconds and identified its extra pot and shifted framing. Final code must normalize only review inputs, retain the output pixels, compact correction feedback, reserve its character budget, and pass fresh positive/negative controls before the next release. The intermediate candidate passed 382 Python tests and the full type check; later edits need focused revalidation.

The frozen candidate `generated_assets.py` SHA256 is `84eb0824fd8b955efdad7bb4c8606560f589ddacd02a908c1515973ad84c0629`. Its real positive review passed in 8.94 seconds and the old-image negative control remained rejected in 11.27 seconds. Original assets and the eight product references are unchanged; only the two review canvases are normalized. The source inventory and generated-image count remain independently classified.

The factual prompt is encoded independently of feedback, with 2,800 UTF-16 characters reserved before the first image request. Feedback contains only expected count, four checks and issues; its final JSON size is verified without silent truncation. The catalog first prompt is 24,416 characters, current feedback prompt 25,356, and a maximum-size allowed feedback prompt 27,216. The full-page machine narrative is omitted once the photographic crop audit is available. All eight products, forty condition associations, keep/avoid and source audit data were recovered exactly from the final prompt. The focused suite passed 85 tests and the full type check reported zero errors.

## Complete catalog geometry

Builder v5 was deployed with all 164 actual file hashes verified. Job `7e42182c-ae45-4e54-93a8-dbb5d9d91cad` generated and accepted both home and catalog scenes on their first attempts. Home rendering passed. Catalog HTML failed at 1,466px height: the design depicts seven cards in single-row groups `[2,2,3]`, but the factual catalog has eight products grouped `[2,2,4]`. Retaining the old three-column final group forced an extra row. The existing 135% complete-page height gate correctly rejected it.

The v6 candidate reconciles corresponding single-row collection columns to actual membership counts while preserving observed cardCounts, factual memberships, existing multirow and unaligned cases. Its normal generation/repair prompt allocates proportional group widths, preserves photograph geometry, and overlays live captions in the photo's empty region. Mobile captions must move into normal flow and remain visible. It does not relax any render gate.

The first isolated catalog HTML test corrected desktop geometry to 1,229px but still hid its mobile caption. After the explicit mobile-label instruction, the second normal two-pass HTML test passed all desktop/mobile checks in 47.09 seconds, with eight cards in `[2,2,4]` single rows and 1,237px desktop height. Both tests reused the verified v5 material with zero IMAGE calls; no customer HTML was manually patched. The v6 logic passed 395 full Python tests; the final mobile prompt change then passed 44 focused tests and the full type check. Actual screenshots, source HTML, module hashes and metrics are retained under `catalog-layout-canary-v2/`.

## Accepted scenes survive website rebuilds

The v6 normal job `4408a121-ce34-4299-9169-b8a7e441c983` failed after two home image attempts, before HTML. Its source audit counted three bottles, but the review cited a hardcoded four-bottle example as a requirement. The example is replaced with per-object classification language; actual count, identity, composition, completeness and UI checks remain unchanged. The second rejected image also changed framing, so neither rejected output is accepted retrospectively.

The larger repeated-spend cause was verified: accepted photographic scenes were only reused inside a single job. Every website rebuild regenerated them. The new private scene cache keys the entire page design image, all original reference assets, draft facts/constraints (excluding only build metadata in siteDesign), and image model/base. It saves a complete accepted page scene set before HTML. Fresh page text/layout planning still runs; only accepted photographic boxes and bytes are reused, with new asset IDs and explicit source-job/hash evidence. Missing entries may generate; corrupt or incompatible entries stop explicitly. No database schema, public route or environment change is needed.

The exact SQLite v5/v6 input bodies were retrieved read-only. Controlled local seeding validates source crops, accepted status and asset checksums before importing the existing home/catalog materials. New About/contact materials used the normal provider helpers and exact v6 inputs; contact passed its first image in 51.64 seconds, while About corrected a bottle time marking and passed its second image in 118.09 seconds. Actual source/output images were inspected. These accepted assets will be identified as reused in the next build, not described as newly generated by that production job.

The final cache candidate passed 427 full Python tests in 35.13 seconds, 67 focused tests and Pyright with zero errors. Independent review passed 117 relevant tests and used actual assembly to verify scene ID remapping preserves exact bytes in both HTML passes. A corrupted cache stopped before image or HTML calls. The review found and resolved a merged crop-area boundary; no render gate was relaxed. Deployment and the next normal production build remain separate acceptance steps.

## Full render feedback and private iframe media

Builder v7 and the four private caches were deployed with all 165 actual file hashes verified. Job `4cb2e8ab-5d2e-4a6b-aa30-9652afd7fcd8` was submitted once by logging into the real production browser and clicking the ordinary website-generation button. It reused all four scenes, passed home/catalog checks, then failed detail mobile layout. The initial detail had an approved-content-order violation, causing assessment to return before desktop/mobile rendering. Its only repair therefore never saw the eight-column mobile overflow and hidden card images.

The v8 candidate accumulates that specific content-order error during safe diagnostic assembly and attaches actual screenshots/metrics to the same repair request. The error remains fatal for page acceptance; default final assembly, sanitizer and other contracts stay strict. Replaying the exact failed initial HTML through one real text-model repair passed all checks in 42.11 seconds, with zero image calls and no manually edited output. Six regressions include actual two-size rendering and prove incorrect order still cannot pass build_page or final assembly even with otherwise clean render metrics. Full validation passed 433 Python tests in 36.41 seconds and Pyright with zero errors.

A separate customer-preview defect was verified in the actual public frontend bundle: its src/poster rewrite removed embedded raster images, including generated scenes and logo crops, before the iframe. The minimal Preview.tsx change preserves PNG/JPEG/WebP data images, keeps same-project private assets on the existing authenticated Blob bridge, and removes unsupported sources. Real browser decoding with the actual generated WebP and private product image verified both data and Blob paths under the existing CSP/sandbox. Frontend regressions first reproduced five failures, then the full npm check passed 244 tests, type checks and Vite build. An isolated build of the pre-change source matched all seven production assets byte-for-byte; the release can therefore update only this frontend asset set while preserving deployed Worker code, bindings and observed asset-routing configuration.

Builder v8 and the narrow frontend asset update are authorized after these checks. A new normal production build and actual five-page visual/iframe acceptance are still required; the failed v7 job and public customer release remain unchanged.

## Explicit missing-content feedback

Builder v8 and frontend version `148c093a-5ba7-429e-83a2-f69d3e39c16c` were deployed and verified. The frontend update preserved Worker code SHA `901beacbdedb91027a87916edb3406f12007165b60475cfcb12d6d07c41a5ee6`, all thirty bindings and actual asset-routing configuration; all eight served asset hashes matched. Normal browser job `8b81bf87-0bc7-4248-ae31-3da5470560c0` again passed home but failed detail: both initial and reviewed HTML omitted `section.1.body`. The repair moved section zero into the correct position but never supplied the missing specification paragraph. Generic feedback said only that required bindings were missing, without identifying which binding. Both original files and messages are preserved.

The v9 change is restricted to assembler.py. Order feedback now lists exact expected and observed sequences, and missing-binding feedback lists exact IDs. Diagnostic rendering may continue when only approved page/section text is missing, with the same error retained as fatal; product/image, collection/form structure and safety requirements still stop. Replaying the exact failed v8 initial HTML with one real text repair supplied the missing paragraph and passed all checks in 50.9 seconds, with zero image calls. Actual desktop/mobile screenshots were inspected. Seventy-two focused tests and the full 437-test Python suite passed, as did Pyright. Frontend code and the four accepted scene cache records are unchanged. A narrow Builder v9 release and one new normal build are authorized; full-site acceptance is still pending.

## Preserve a valid candidate when review regresses

Normal browser v9 job `79630aff-53b2-43bf-8bf6-7d4ae6beaf15` reused accepted scenes and passed its catalog initial checks. The mandatory visual review enlarged product images to 118% and removed their max-width, introducing mobile overflow: the exact initial rendered at 390px with no outliers, while the reviewed candidate rendered at 393px with all eight image boxes extending from -3.23 to 393.23. The program always selected the second candidate and therefore failed the entire build despite retaining a passing original. Both candidate HTML and screenshots remain unchanged in runtime-v9.

The v10 candidate changes only builder.py. It still performs exactly one visual review and all existing assembly, render and visual-plan checks. A reviewed candidate with known failed checks may select the initial candidate only when that initial candidate passed every check. Two invalid candidates still fail; provider timeouts and unknown exceptions propagate. Separate selected evidence records which original candidate was returned and why; it does not overwrite the reviewed evidence or manually repair customer HTML.

The v9 detail sibling was canceled during its review when catalog failed. Replaying its exact initial HTML with one real text repair passed every check in 61.67 seconds with zero image calls; source and desktop/mobile output remain under detail-v9-repair-canary. This canary does not replace a full production build. Exact v9 input and runtime-cache audit confirmed four cache hits and all 165 live file hashes. The public customer release remains unchanged.

The exact v9 catalog candidate replay passed real assembly/render checks in 2.8 seconds with zero network calls, selecting the byte-exact initial HTML. Full Python validation passed 447 tests in 36.17 seconds; focused selection/diagnostic review tests passed 22 and Pyright reported zero errors. The v10 deployment is restricted to builder.py over the immutable v9 package. Full normal browser acceptance remains pending.

## Decorative breadcrumb false positive

Builder v10 was deployed with all 165 actual runtime hashes verified. Normal browser job `5acf56af-7137-4d19-b834-a94ae834b2f4` reused all four accepted scenes, selected passing home/catalog reviewed candidates and produced a passing About initial. Detail review fixed duplicated bindings and page height, leaving only a generic unbound-text error. Exact parsing of both candidate files found precisely two `<span>&gt;</span>` breadcrumb separators, directly between trusted navigation and current-page bindings. Every other reviewed detail check passed. The failed job and its original HTML are retained.

The v11 correction recognizes only this standalone greater-than leaf span between trusted navigation siblings. One shared predicate preserves it during sanitation and excludes the same decorative token from the unbound-text error. Arbitrary words, mixed text, other symbols, unknown bindings and invalid structure retain existing rejection. It adds no model calls and makes no manual change to generated customer HTML. Acceptance must replay the exact v10 candidate through actual assembly/render checks before deployment.

The exact v10 initial/reviewed detail replay passed real assembly, desktop/mobile rendering and strict final assembly in 2.98 seconds with zero network/model/image calls. Selected HTML remains byte-exact reviewed output; the two separators are restored only by the general trusted sanitation boundary. Final geometry was 1,329px at desktop, eight cards and one form, with no failed checks. The complete Python suite passed 464 tests in 36.35 seconds; 37 focused tests and Pyright zero errors also passed. The narrow v11 release is authorized; the next normal five-page build remains required.

## Preserve validated pages and identify exact unbound nodes

Normal production v11 job `4be12189-617a-4542-9a62-cfab838b0bd9` again reused all four scenes and passed home/catalog. Detail failed on duplicate approved text and decorative required-field stars. The bounded repair feedback now lists up to twelve exact unbound nodes with tag, class and bounded text. Instructions remove duplicate elements entirely and preserve trusted form controls without unsupported raw stars. Replaying the exact failed initial detail with one real text correction passed in 31.33 seconds, with zero image calls.

Repeated builds were discarding already accepted page work. A private page cache now saves only candidates that pass assembly, actual desktop/mobile rendering and visual-plan checks. Its identity includes draft facts excluding siteDesign build metadata, all approved designs/references, provider/model identity and the exact shared homepage HTML for inner pages. Every cache hit repeats current checks before use; corrupt or newly invalid entries fail explicitly before paid fallback. Valid cached pages are checked before missing pages enter paid planning. Successful sibling pages persist even when another page fails. There is no database schema, route, authorization or environment change.

Three unchanged accepted pages were seeded after independent zero-network revalidation: v11 home/catalog and the detail correction. Their original source evidence, hashes and source job identities are preserved. The combined changes passed 512 Python tests and Pyright. Builder v12 was staged as an immutable 166-file package, SHA256 `867b74c5cdf759a9bd141b3c5644d40b9a0ac84da95f219c007acf27f07d80f3`, but was not activated. Production remains v11 until full recovery passes.

The first missing-page canary stopped on a text-provider connection error through the local system proxy, without any image call. Authenticated read-only model-list probes then returned HTTP 200 through both configured and direct paths. A separately guarded direct canary reused the three accepted pages and all four scenes, generated only About/contact, and stopped on About's image-occlusion check. Its contact review was canceled rather than silently continued.

## Sample painted image geometry

The unchanged About reviewed screenshot shows complete bottles. Exact isolated browser geometry found a 1121.266×472 image element with `object-fit:contain; object-position:right bottom`, but the 1536×752 bitmap paints only 964.085×472 pixels starting at x571.915. The old image-element sampling grid puts its first column at x638.988, under the live hero copy; the bitmap's 20% column is x764.732 and all nine bitmap samples hit the actual image. The fix must sample painted image coordinates while retaining the existing seven-of-nine threshold and opaque full/partial obstruction controls. It must not ignore arbitrary overlapping text or relax content/cropping checks. The original HTML and photographic pixels remain unchanged.

After this fix, replay the saved About candidates with zero providers, complete contact using its saved initial plus one real review, and verify two whole twelve-route builds with all providers forbidden. Import only the five accepted page-cache records with original provenance into a new immutable builder release. A fresh production browser button submission, actual five-page desktop/mobile private iframe acceptance and side-by-side screenshots remain mandatory. Known typography, logo-crop and extra approved-copy differences must not be represented as pixel-perfect reproduction.

The final geometry correction passed 29 focused renderer tests, including full/partial opaque obstruction controls. Exact About replay then passed in 1.81 seconds with no network or provider calls. Contact replay plus one real review passed in 71.2 seconds (three internal text HTTP requests, all 200; no planning or IMAGE). The complete 518-test Python suite passed in 39.41 seconds. Two complete five-page/twelve-route warm builds passed every current check in 3.59 and 3.52 seconds with zero providers/network, byte-identical files and no database creation. These are cached-recovery timings, not first-generation timings. Builder v13 deployment is restricted to frozen builder.py, new page_cache.py and renderer.py over the verified immutable v11 runtime; the existing public bottle release remains unchanged.

## Brand-mark pixel boundary found during visual acceptance

Independent review of all ten screenshots found a shared P1 logo defect despite clean render metrics. The homepage logo crop `home:48,16,95,23` maps to pixels (74,16,220,40), while the original mark begins at x73 and its g descender ends at y47. Every page inherited this crop and visibly changed the brand spelling. Builder v13 was staged and its private provenance archive uploaded, but cache import and runtime cutover were paused before execution; production remains v11.

The next change is limited to design_assets.py and its tests: recover edge-connected logo ink only when a consistent plain background and an edge contact can be established, within a small capped search window. Complete logos remain byte-identical, scene/icon extraction remains unchanged, expanded area still counts against the original crop budgets, and unrecoverable bounded contacts fail explicitly. Do not hand-edit page HTML or modify accepted page/scene caches. Revalidate all five unchanged cached pages with the new materialization before a new immutable v14 deployment. Retain the v13 source/provenance archive without overwriting or needlessly retransmitting it.

Other visual differences remain documented as P2: compact/small hero icons and product scale, catalog group alignment and caption layout, detail-photo whitespace and extra approved-copy sections, and the shared dark simplified footer. They do not establish pixel-perfect equivalence and must be shown in the final comparison.

The bounded logo correction recovered the exact original mark from (74,16,220,40) to (71,16,220,50), with no new image generation or page editing. Final design_assets.py SHA256 is `c50b2f7505c68e7a040a4146eb946f45c638d387f34c0a79216304b8d52b27b2`. Thirty-seven focused asset tests passed, followed by 529 complete Python tests in 40.58 seconds and Pyright with the configured Python interpreter at zero errors/warnings. Two complete current-cache builds passed in 4.30 and 3.71 seconds, with zero network/providers and identical twelve-route files. Comparing v13 and v14 assembled outputs confirmed only the shared logo data URI changed in all twelve routes; scene pixels, page HTML caches and factual content remained intact. Actual desktop logo now reads senseng correctly. The guarded v11-to-v14 application deployment and preserved-source cache import are authorized; real production browser submission/preview acceptance remain pending.

## Actual v14 deployment and ordinary browser rebuild

Builder v14 is live at PID 1326558, restarts zero, package SHA256 `4428d94d4787ff8391b2cb3ee693e83b1317929db8b822b8025b3f8bd81c4406`. All 166 runtime files match. Relative to the former live v11, the cumulative runtime changes are builder.py, new page_cache.py, renderer.py and design_assets.py. The v14 package's one-file allowlist is relative to staged v13, not relative to former production. Five page records and four accepted scene records retain their source provenance and hashes. All thirteen preexisting database rows/schema, loaded environment, Product Radar, Worker version/code, thirty bindings and eight frontend assets were preserved. Health/auth checks passed. Deployment created no jobs and made no provider calls.

The actual production login and ordinary website-generation button submitted exactly one job, `cb9e2dfa-9a64-4752-89e9-a2a4e5135468`. It succeeded, with Worker creation-to-update time 51.028 seconds. Remote selected evidence shows five cached pages and four reused scene assets. The twelve final HTML files are byte-identical to the v14 warm validation output. This is a real online recovery build using reviewed source pages/materials, not a first-generation timing claim.

The exact production preview HTML and eight original product assets were retrieved through authenticated APIs and served locally with inquiry sending disabled. All twelve routes and original product headings/photos passed; four standalone scene files match their accepted hashes. No broken images, empty bound labels, empty icons or mobile horizontal overflow were found. Approved company/products/copy/languages/primary product/consultation and the existing public release remain unchanged. Five desktop/mobile screenshots and an interactive three-way comparison were produced; its five tabs and four-image-per-tab loading, plus the four-material page, passed actual browser checks. These checks do not replace the pending actual application iframe check.

The first iframe-QA script stopped while reading an 11,762,780-byte project response from Chrome's debugger response cache, before opening the iframe. This is recorded as a harness read failure with no iframe acceptance conclusion. Its single bounded follow-up adjusts only the QA debugger buffer/response-read timing; no application code or generated output is changed and no build or model is repeated.

The bounded follow-up passed the actual authenticated application preview for job `cb9e2dfa-9a64-4752-89e9-a2a4e5135468`: all five pages in desktop and mobile modes, actual iframe widths 1490 and 378 pixels, scrollWidth equal to each viewport. Every generated scene ID matched its exact preview response and decoded from inline data; every private product image decoded through the authenticated Blob bridge. Both forms and eight-product collections were present. Ten actual UI screenshots and result.json are retained under online-preview-after-fix-v2. Root inspected the real home desktop and contact mobile UI; the shared senseng logo is complete. No inquiry, publication or additional generation was submitted.

Final local preview is http://127.0.0.1:58465/en/, comparison is /comparison/ and independently downloadable materials are /materials/. The service listens only on loopback. acceptance-summary.json separates application release, actual job/iframe acceptance, static artifacts, known visual differences, preserved public release and the fact that no Git commit/push was performed. The initial-design 28,000-character guard and standalone-material pipeline are deployed; this final 51.028-second job reused accepted pages/materials, so its timing does not represent a new first-generation build.
