# 产品身份实现验收记录

## 实现验证

- Product Radar TypeScript 全量：3,589 项，3,578 通过、7 跳过、4 个已确认的既有失败。本次新增/修改功能回归通过。
- 既有失败分别为 dashboard-market-analysis-route 的认证错误附加 code、language 的商品占位词旧断言、product-creation 的零额度旧断言、social-insight-route 的固定日期断言。未为了本次发布改写这些行为或削弱断言。
- Product Radar Python：98 项通过；类型检查、生产构建、14 个公共产物检查通过。
- Web Radar：合并同事同期上线的 10 个模板并冻结新渲染器后，1,616 项全部通过；类型检查和生产构建通过，Worker dry-run 成功。
- 两仓真实路由集成在最终39模板版本重新通过：合同、素材确认、单次额度、重试、私有预览、发布模拟、版本并发和隔离。
- 浏览器验证：桌面 1440 和手机 390 像素，默认整套5张、上限10张、11张非法；6张生成成功加原图明确显示7/10，失败8/9/10位置与原因可见，无横向溢出和JavaScript错误。
- 未调用付费生图服务，未恢复旧 Ocean 批次。浏览器使用本地真实组件与测试数据，不能替代新生成图片的视觉验收。

## 发布边界

本记录只证明上述代码与构建检查。正式上线状态以最终交付的 Git 提交和线上版本核对结果为准。两仓保持独立；不包含被用户拒绝的 SaaS UI 改版，不重发客户网站，不迁移数据库。

- 发布基线追踪：Web Radar 线上版本 `5547ee7e-bdd5-4741-9fc0-1f4611e0c30e` 经完整 Worker 代码核对对应同事 `bafe6c1`；本次合并保留整个提交。新旧行业合同及五个页面输出一致，未来独立模板修改不能改变已固定的身份版本。
